# Build output
/build/
/build-voice-test/
/build-root-check/
/build-voice-test-asr/
/build-voice-test-stream/
/build-voice-test-tts/

# IDE caches
CMakeSettings.json
CMakeUserPresets.json

# Python (training scripts / venv)
/venv/
**/__pycache__/
*.pyc

# Large model files (tracked externally / via model downloader)
/models/
/WineFoxModel
# Third-party sources managed by git submodule or downloaded
# (submodule gitlinks must be tracked; contents are managed by each submodule's own .gitignore)
/tts-training/littlemaid_peco/
# CosyVoice: bundled model weights + python venv (re-download to populate)
/tts-training/CosyVoice-0.5B/


# Pre-built llama.cpp binaries (kept outside source tree)
/llama-b10017-bin-win-cpu-x64/
/llama-b10017-bin-win-vulkan-x64/

# ---------------------------------------------------------------------------
# voice-test sandbox
# ---------------------------------------------------------------------------

# Build artifacts
/voice-test/build/

# ONNX / GGUF / .bin model weights — large, downloaded via scripts/
/voice-test/models/

# Test data: audio files, numpy dumps, ASR/VAD output texts
/voice-test/test-data/

# Pre-built ONNX Runtime binary (re-download via CMake fetch)
/voice-test/third_party/onnxruntime-prebuilt/

# Runtime feature config (regenerated by sensevoice-cpp scripts)
/voice-test/fbank_lfr_cmvn_feature.json
/fbank_lfr_cmvn_feature.json

#tts-training data
/tts-training/dataset-kokoro-test/
/tts-training/dataset/
/tts-training/kokoro-model/
/tts-training/kokoro-src/
tts-training/voices-v1.1-zh.bin
/tts-training/voice_finetune/verify_output/
/tts-training/voice_finetune/output/
# ---------------------------------------------------------------------------
# Runtime data
# ---------------------------------------------------------------------------
*.db
*.db-journal
*.db-wal
*.db-shm
logs/
*.log
*.log.err
winefox.json

# OS junk
Thumbs.db
.DS_Store
desktop.ini
